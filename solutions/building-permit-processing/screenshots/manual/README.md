# Literal browser manual-mode evidence

`browserfilm.json` is the ordered authority for 24 real browser frames.
`manual-build-walkthrough.gif` and `manual-build-contact-sheet.jpg` summarize those frames when the files are present.

Historical sequence: the step-16 failure and step-17 retry captures, their annotations, the GIF, and the contact sheet are preserved byte-for-byte. They do not require a validation error or prove the current upload outcome. Follow the frame tutorial contracts and record what Copilot Studio actually does. Never corrupt a valid skill or manufacture an error. Fresh outcome and inventory captures are required for steps 16 and 17.

Do not replace a missing capture with a generated image or describe a pending
asset as evidence. The package uses synthetic inputs and qualitative language;
no frame is a customer KPI, production result, or publication approval.
