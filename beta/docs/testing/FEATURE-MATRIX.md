# RAPP Brainstem Frontier (beta/) — Complete Feature Inventory

Source of truth: `beta/` at commit `81337225` (`fix(frontier): redact sensitive worker output`, 2026-08-20). Version `0.1.0-beta.7`. Read-only sweep; nothing under the repo was modified.

Conventions used below:
- `betaHome` = `$BRAINSTEM_BETA_HOME` or `~/.brainstem/beta-launcher` (`$BRAINSTEM_HOME` overrides `~/.brainstem`).
- `brainstemDir` = `$BRAINSTEM_BETA_SOURCE_DIR` or `~/.brainstem/src/rapp_brainstem`; `python` = `$BRAINSTEM_BETA_PYTHON` or `~/.brainstem/venv/bin/python`.
- `lineageRoot` = `$RAPP_LINEAGE_HOME` or `~/.rapp/lineage`.
- "Model?" = does exercising the feature end-to-end require a real GitHub Copilot model (Surgeon SDK session and/or the Grail kernel's `/chat` LLM call). "no (CLI)" = no model call, but the bundled Copilot CLI / sign-in must exist for the "ready" phase.
- "Static test" = the test reads source text with regexes (contract/lint style), it does not execute the behavior.
- Test references are `file: "test name"`; all under `beta/tests/`. Scripts are under `beta/scripts/`.

---

## 1. Feature table

### 1.1 Electron shell & window lifecycle (`electron/main.mjs`)

| # | Feature | Area | Entry points | State touched | Model? | Existing unit/integration tests | UI-driven coverage (script) | Concurrency-sensitive | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Single-instance lock; second launch focuses/restores existing window | shell | `app.requestSingleInstanceLock`, `second-instance` | none | no | none | none | yes (two launches) | `app.quit()` in the loser; `brainstem-chat`/`surgeon-chat`/walkthrough scripts spawn `launch.sh` when bridge missing, relying on this. |
| 2 | Main BrowserWindow (1280×860, min 900×620, dark bg, sandbox+contextIsolation, preload) | shell | `createWindow()`; `BRAINSTEM_BETA_HEADLESS=1` hides window | none | no | installer-contract: "Electron renderer is isolated from Node"; brandmark: "main wires the brandmark as the runtime window + dock icon" (static) | package-gate (headless smoke) | no | `window-all-closed` quits on non-darwin; `activate` recreates. |
| 3 | App/dock icon = brandmark (`build/icon.png`) | shell/brand | `appIcon`, `app.dock.setIcon` | none | no | brandmark.test (7 tests, static+asset checks); style-guide.test | none | no | Icons regenerated from `build/icon.svg`. |
| 4 | Navigation guards: main frame only `ui/index.html`; subframes only loopback Brainstem or any `127.0.0.1/localhost` twin; external `https:`/`vscode:` → `shell.openExternal`; window.open denied | shell/security | `will-navigate`, `will-frame-navigate`, `setWindowOpenHandler` | none | no | installer-contract: "embedded VS Code link opens externally without replacing Brainstem" (static) | none | no | `isTwinFrameUrl` allows ANY loopback http(s) subframe, not just known twins. |
| 5 | Trusted-IPC gate (`assertTrustedIpc`) — sender must be main frame at `uiUrl` | shell/security | every `ipcMain.handle` | none | no | none | none | no | Rejects IPC from the Brainstem iframe / twin frames. |
| 6 | Native application menu (darwin: app/Edit/View/Window; others: File/Edit/View/Window/Help) incl. **Check for Updates… `CmdOrCtrl+Shift+U`**, reload/forceReload/toggleDevTools/zoom/fullscreen roles | shell | `installApplicationMenu()` | none | no | installer-contract: "desktop menu checks GitHub for source updates" (static) | none | no | Menu item disabled while a check is in flight. |
| 7 | Media permission handler — only grants `media` to the window while `start_recording` fallback is running | shell/security | `session.defaultSession.setPermissionRequestHandler` → `allowsUiDriverMediaPermission` | none | no | none | none | no | `recordingPermissionWebContents` set only during fallback MediaRecorder start. |
| 8 | Smoke-exit timer | shell/test hook | `BRAINSTEM_BETA_SMOKE_EXIT_MS` | none | no | none | package-gate.mjs (3000 ms) | no | Used by packaged-app headless smoke. |
| 9 | Graceful shutdown: stop all Surgeons, twins, Copilot client, route manager workers, UI driver; then quit | shell | `before-quit` (two-phase) | removes `ui-driver.json` (owned), kills workers, removes retired composition dirs | no | none | none | yes (shutdown during loop/record) | `Promise.allSettled`; no timeout on the whole sequence. |
| 10 | Startup services: default route start + Copilot CLI start → `state.brainstem/copilot/surgeon/uiDriver/update` phases broadcast over `beta:state` | shell | `startServices()`, `emitState()` | `betaHome/routing/**`, `logs/workers/*.log` | no (CLI) | brainstem-process.test (config/health); copilot-runtime.test | walkthrough scripts wait on `#input` | yes (route start vs. first IPC) | Phases: starting/ready/error/signed-out/warning. |
| 11 | Startup fingerprints (beta source via git; Brainstem runtime dir) published in `ui-driver.json` | provenance | `betaSourceFingerprint`, `runtimeDirectoryFingerprint` | reads repo + `brainstemDir` | no | walkthrough-provenance.test: "runtime fingerprint detects code changes and excludes secrets" | walkthrough-gate (binds evidence to fingerprints) | no | Git required; falls back to tree walk. |
| 12 | Export-redaction script injected into the Brainstem frame (wraps `window.Blob` for JSON exports) | privacy | part of `BETA_FRAME_BRIDGE_SOURCE` | none | no | log-redaction.test: "Frontier JSON Blob exports are scrubbed before download" | none | no | Scrubs credentials, local paths (`brainstemDir`, home, tmpdir), emails, IPs, URL queries. NEW in 81337225. |

### 1.2 Brainstem iframe bridge (`BETA_FRAME_BRIDGE_SOURCE` in `main.mjs`, installed via `beta:install-frame-bridge`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 13 | Bridge install on every iframe `load` (idempotent via `window.__rappBetaFrameBridge`) | bridge | renderer `frame.addEventListener("load")` → `installFrameBridge()` | none | no | installer-contract: "beta launcher reuses the global Brainstem without duplicate toolbar IPC" (static) | all drive scripts implicitly | yes (route change reloads frame) | Finds first subframe whose URL passes `loopbackUrl` (must match active `state.url` host+port). |
| 14 | Agent list decoration: humanized agent titles, icon buttons for export/delete (MutationObserver on `#agent-list-ul`) | bridge/UI | `decorateAgentRows` | none | no | agent-display.test: "agent display names split CamelCase while preserving acronyms" | none | no | Relies on Grail `.agent-title`, `.export-btn`, `.del-btn`. |
| 15 | Brain logo (`header .logo`) toggles the Explorer (click / Enter / Space; `aria-expanded` synced) | bridge/UI | postMessage `rapp-beta:toggle-explorer` / `rapp-beta:explorer-state` | localStorage `rapp-brainstem-beta-explorer-open-v1` | no | installer-contract: "blue Brainstem icon toggles the live agents Explorer" (static) | walkthrough-via-chat clicks `header .logo` | no | Title "we are above that". |
| 16 | Three-dot **RAPP Brainstem Frontier** menu injected into Grail header (`#beta-app-btn`, `#beta-app-panel`, `#beta-check-updates`, `#beta-install-update`, `#beta-update-status[data-phase]`) | bridge/UI | postMessage `rapp-beta:check-updates`, `rapp-beta:install-update`, `rapp-beta:open-update`, `rapp-beta:update-state` | none | no | installer-contract: "desktop chrome omits the redundant wrapper toolbar" (static) | drive-via-chat (clicks `#beta-app-btn`, `#beta-check-updates`, reads `#beta-app-panel`) | no | Phases rendered: idle/checking/current/available/blocked/applying/success/error. Closes on outside click. |
| 17 | Molt-Lineage safe-word interceptor: wraps `window.fetch` for `POST /chat` and `/chat/stream`; exact trimmed `baseline`/`restore` (env-configurable) short-circuits to main; fail-OPEN on any error/timeout (120 s) | bridge/lineage | `requestLineageCommand` → `rapp-beta:lineage-chat` → `beta:lineage-command` | `lineageRoot/**/HEAD`, `PRIOR_HEAD`; restarts default route | no | lineage-control.test (9 tests incl. "renderer intercepts before Grail chat and main exposes the lineage IPC" static); lineage-store.test; molt-lineage-integration.test | none | yes (route swap mid-chat) | Synthesizes SSE `done` frame for `/chat/stream`. |
| 18 | Lineage confirmation reply re-delivery after the route URL changes (pendingLineageReply + ack) | bridge/lineage | `rapp-beta:lineage-confirmation` / `-ack` | none | no | none (only the wire shape in lineage-control.test) | none | yes | Appends via Grail `appendMsg` if present else raw DOM. |
| 19 | Export agent.py to `~/Downloads` (dedupes with ` (n)` suffix) | bridge/agents | `.export-btn` click → `rapp-beta-export-agent` → `beta:export-agent` | `~/Downloads/<file>.py` (0600) | no | none | none | no | Reads from the ACTIVE composition dir, not the global source. |
| 20 | Delete agent (confirm dialog) → `removeActiveAgent` → restart default route | bridge/agents | `.del-btn` → `rapp-beta-delete-agent` → `beta:delete-agent` | global: `rm brainstemDir/agents/<file>`; stack: stack JSON; restarts worker | no | route-manager.test: "active-agent deletion persists for stack and global sources" | none | yes (route swap) | Memory/ephemeral scopes refuse. Writes into the user's real `~/.brainstem/src/rapp_brainstem/agents`. |
| 21 | `?beta=1` query appended to the Brainstem frame URL | bridge | renderer `betaUrl()` | none | no | none | none | no | Grail reads it to hide its own toolbar wrapper. |

### 1.3 Renderer shell (`ui/index.html`, `ui/renderer.js`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 22 | Splash card (“Starting your global Brainstem”) + `#error` text from `state.brainstem.message` | shell UI | `render(state)` | none | no | none | none | no | Hidden once `brainstem.phase==="ready"`. |
| 23 | First-run intro overlay (`#intro`, `#enter`) with interview prompt `#show-mode-interview-prompt` and Show Mode button | onboarding | `#enter` click | localStorage `rapp-brainstem-beta-intro-v1` | no | installer-contract: "first-run guide explains the customer rapid-use-case loop" (static) | drive-via-chat / walkthrough / show-mode-capture click `#enter` (optional) | no | Entering also opens the Surgeon panel. |
| 24 | Explorer panel open/close/refresh (`#explorer`, `#explorer-refresh`, `#explorer-close`); responsive (<820px closes Surgeon) | explorer | buttons; logo toggle | localStorage `...-explorer-open-v1` | no | none | walkthrough reads `#agent-tree` | no | `body.explorer-open`. |
| 25 | Explorer tree: `RAPP/1 ORGANISM` root, identity rows (caller/memory/composition), `agents/ · shared + memory` folder, `stack RAPPIDs` tree with `selected`/`overlay n`/`n agents`, per-file rows (`hot-load`/`support`/`role`), 2 s polling, signature-diff re-render | explorer | `refreshAgentExplorer()` via `beta:list-agent-files` | reads `routing/**` | no | none | walkthrough-gate: "stack tree has a named selected stack", "ephemeral source is absent from final Explorer" | yes (poll vs. route swap) | Only polls when explorer open and brainstem ready. |
| 26 | Agent source viewer (read-only `<pre>`), revision-aware refresh, auto-deselect when file leaves composition | explorer | row click → `beta:read-agent-file` | reads active composition `agents/<file>` | no | none | none | no | Tab label `agents/` vs `agent_stacks/`. |
| 27 | Surgeon panel open/close (`#surgeon-tab`, `#surgeon-close`), persisted | surgeon UI | buttons | localStorage `...-surgeon-open-v1` | no | ui-driver-server.test: "Brain Surgeon driver opens an existing off-canvas panel without a visible tab" | `surgeon_chat` drive action opens it | no | `body.surgeon-open`. |
| 28 | Surgeon composer: autosize textarea, **⌘/Ctrl+Enter** to send, `Build`/`Working` button | surgeon UI | `#surgeon-input`, `#surgeon-send` | none | yes (send) | none | surgeon-chat.mjs, drive-via-chat, walkthrough, frontier-acceptance | no | Busy tab blocks; use `+` for parallel. |
| 29 | Starter pills (5): build/test, inspect, check updates, record demo, **Show Mode click-through** (tour), **Deploy loaded agents to Copilot Studio** (long scripted prompt) | surgeon UI | `.surgeon-starters button` | none | yes except tour | show-mode-tour.test: "the click-through sits beside the interview loop and never sends a prompt"; installer-contract: "beta embeds the full GitHub Copilot Brain Surgeon loop" (static) | none | no | Deploy prompt names tools: ensure_copilot_studio_deploy_agents, copilot_studio_deployment_defaults, copilot_studio_auth_status, list_active_agent_files, start_copilot_studio_login, show_copilot_studio_agent_link. |
| 30 | Multi-chat sessions: tab strip (`#surgeon-tabs`), `+` new, `×` close, per-session transcript node, persisted history (last 80 msgs; quota fallback 4×24), restore on load, active id | surgeon multi-chat | tabs | localStorage `...-surgeon-sessions-v1` | no | surgeon-multichat.test: "renderer models several Copilot chats as tabs + a herd grid", "the shell has the tab strip, herd button, and wrapping herd grid" (static) | none | yes (parallel sends) | Main caps at 12 sessions (`MAX_BRAIN_SURGEONS`). |
| 31 | Reset active chat (`#surgeon-new` ↺) → clears transcript + `beta:surgeon-reset` | surgeon | button | releases chat leases | no | none | none | yes (reset mid-delegation) | Error bubble on failure. |
| 32 | Surgeon event rendering: streaming `delta`, `tool-start`/`tool-complete` cards (`.surgeon-tool`), `artifact` cards (image/video/link), `lease` bubble, `done`, `error`, `reset` | surgeon UI | `beta:surgeon-event` (tagged `sessionId`) | none | yes | none | walkthrough/drive scripts read `#surgeon-log .surgeon-message.assistant` | yes | Events for unknown session fall back to active session. `tool-complete` triggers explorer refresh. |
| 33 | Herd view (`#surgeon-herd`): grid of session tiles, `+ New chat`, `Dock ▸`, `◈ Hatch a RAPPlication…` | herd | `#surgeon-herd-btn` ⊞ | none | no | surgeon-multichat.test (static); twin-manager.test: "the herd renders a chat/work-log tile per twin" (static) | frontier-acceptance reads herd text via `inspect target:shell` | yes | `body.surgeon-herd-open`; twins force-open the herd on hatch. |
| 34 | Store picker: source tabs (AIBAST RAR / Frontier Store / Public RAR / Custom…), custom URL input, rows with `◈ Hatch` + `⇣ Install`, yanked filtered out, gated disabled, transient `.herd-error` banner | store UI | `.hstore` button | `betaHome/store-source.json` | no | twin-loop-hardening.test: "store-picker hatch failures are surfaced to the user, not swallowed" (static); rapp-store.test (client) | none | no | Network fetch of catalog. |
| 35 | Twin tile: `◈` badge, title, status pill (`working`/`done`/`sign in`), `⟳ Loop`, `⤢ App`, `×`, meta line (`:port · license · rappid`), multiplayer transcript (activity / `YOU` / `BRAINSTEM` driver / twin), `Sign in to continue →`, composer | herd/twins | `twinTileFor` | none | yes (sending) | twin-manager.test: "small view = a chat/work-log…" (static); twin-loop-hardening.test: "the renderer distinguishes the human (self) from driver turns…", "the twin transcript array is bounded" (static) | frontier-acceptance | yes | Transcript bounded to 200 msgs / render 60; loopLog 40. |
| 36 | Twin event handling (`twin-hatched` → open herd; `twin-status` updates only known twins; `twin-log`; `twin-message`; `twin-closed`) + `refreshTwins()` on load | herd/twins | `beta:twin-event`, `beta:twin-list` | none | no | twin-loop-hardening.test: "close() signals an in-flight loop so no ghost tile is resurrected" | none | yes | Late status after close cannot resurrect tile. |
| 37 | `beta:twin-focus` → enter herd + scroll tile into view (from OS notification click) | herd/twins | `onTwinFocus` | none | no | twin-manager.test: "away-auth…" (static) | none | no | — |
| 38 | Egg drag-to-hatch: full-window veil on file drag, drop `.egg` → bytes → `beta:twin-hatch-egg`; dismissable error toast | twins/eggs | window `dragover/dragleave/dragend/drop` | `betaHome/twins/<id>/**` | no | none for the renderer; twin-manager hatchEgg covered indirectly (frontier-rapplication-hardening: "a grown capability survives the launcher clearing the twins root") | none | no | Veil self-heals via 400 ms quiet timer (cross-origin iframe swallows dragover). |
| 39 | Update-panel open request from main (`beta:open-update`) → opens three-dot panel on next sync | updates UI | menu/Surgeon `check_beta_updates` | none | no | none | none | no | `openBetaMenuOnNextSync`. |
| 40 | `window.__brainstemBetaNavigationCount` (iframe load counter) | telemetry | frame `load` | none | no | none | walkthrough checks `no_iframe_replacement` via `route_telemetry` | no | — |

### 1.4 Show Mode (`ui/show-mode-tour.js`, `show-mode.html`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 41 | Show Mode click-through: 14 steps (welcome, interview, pill, doors, import, analyze, edit, approve, preview, test, confirm, promote, why, end) with spotlight hole/ring, ghost content over Explorer/center/Surgeon, synthetic cursor+pulse, progress bar | show mode | starter pill `[data-show-mode-tour]`, intro button, `window.rappShowModeTour.start()`, window event `rapp-beta:show-mode-tour`, `?tour=show-mode`, `#show-mode-tour` | localStorage `rapp-brainstem-beta-show-mode-notes` | no | show-mode-tour.test (6 tests: CSP-safe separate script, walks whole loop, never sends a prompt, standalone replica, driver exposes tour/force_mode top-level, Surgeon+Python driver can light force mode) — static | show-mode-capture.mjs (real window) | no | Opens panes a step needs; restores pane state on exit; emits `rapp-beta:show-mode-tour-ended`. |
| 42 | Tour keyboard: →/Space/Enter next, ← back, N notes toggle, Esc exit; presenter notes panel | show mode | `onKey` (capture phase) | notes key | no | none | none | no | Space/Enter ignored when focus is in textarea/input. |
| 43 | Standalone GitHub Pages replica `beta/show-mode.html` (autostarts unless `?autostart=0`; “Install Frontier →” link) | show mode/docs | URL | none | no | show-mode-tour.test: "the standalone Pages replica drives the same script" (static) | none | no | Static mock of the three panes. |
| 44 | Surgeon tool `show_mode_click_through` (mode walk/capture, pace, force_mode, from_step) + `/drive` `tour` action | show mode/AI | Surgeon tool; bridge `tour` | captures/ | yes (tool call) / no (`tour` direct) | show-mode-tour.test (static) | show-mode-capture.mjs | yes (stage action) | `tour` un-throttles background painting, `moveTop()` without focus steal. |
| 45 | `npm run show-mode:capture` → `docs/show-mode/step-NN-<id>.png` + `index.json` (`--out`, `--pace`, `--force`, `--keep-open`, `--from`) | show mode/docs | script | `docs/show-mode/**`, `betaHome/captures/` | no | none | itself | no | Launches Frontier via `launch.sh` if bridge absent; Copilot sign-in NOT required. |

### 1.5 Visible-UI driver bus (`electron/ui-driver-server.mjs`) — see §2 for the action catalog

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 46 | Loopback HTTP server on a random port; 32-byte bearer `token` + separate `artifactToken`; metadata file written atomically (0600) with pid + fingerprints; removed on stop only if owned | drive bus | `startUiDriverServer`; `ui-driver.json` (`BRAINSTEM_BETA_UI_DRIVER_FILE`) | `betaHome/ui-driver.json` | no | ui-driver-server.test: "long UI commands flush headers and send heartbeats", "long recordings stream to disk without base64 IPC" | every script reads the metadata file | yes (two app instances would race the file) | Metadata carries `brainstemRuntimeFingerprint`, `runtimeFingerprint`, `startedAt`. |
| 47 | Command validation: action allowlist (19), JSON body ≤256 KiB, `run` 1–40 steps, `tour`/`force_mode` top-level only | drive bus | `validateCommand` | none | no | ui-driver-server.test: "visible UI driver accepts bounded user-like actions", "…rejects unknown and unbounded commands"; show-mode-tour.test: "the UI driver exposes tour and force_mode as top-level commands only" | — | no | Nested `run` steps pass validation for e.g. `screenshot`, but `perform()` in-page throws "Unsupported UI driver action" for them (see §2). |
| 48 | Frame targeting: default = active Brainstem subframe (`loopbackUrl`), `target:"shell"` = main frame, `twin:<id>` = twin subframe (via `resolveTwinUrls`) | drive bus | command fields | none | no | twin-manager.test: "the AI can drive a twin's own UI in-tile (P3c)" (static) | scripts use `target:"shell"` | no | Twin must be visible in herd (iframe loaded) else error. |
| 49 | Animated AI cursor, label, click pulse, target outline; cursor fades after 4 s idle | drive UX | `ensureUi()` in `browserDriverCommand` | DOM overlays (`#brainstem-ai-driver-*`) | no | ui-driver-server.test: "synthetic cursor fades after the UI driver becomes idle" (static) | all drive runs | no | Injected per target frame. |
| 50 | Chat lease guard (`set_chat_lease`): per-token lock set; blocks trusted user clicks on `#send` and Enter in `#input`; banner `#brainstem-beta-chat-lease`; `aria-disabled` | drive/lease | Surgeon `acquireChatLease/syncChatLeases/releaseChatLease` | DOM state `window.__brainstemAiDriver.chatLeaseTokens` | no | ui-driver-server.test: "visible chat waits for the stable SSE reply and has no legacy lease hooks"; brain-surgeon.test: "concurrent delegations retain independent chat lease tokens" | walkthrough-gate: "chat lease is released before and after the scenario", "telemetry proves two token-owned lease cycles" | yes (two Surgeons, lease counts) | Synthetic (untrusted) events bypass the guard by design. |
| 51 | Window recording — primary: main-process `capturePage` frames → ffmpeg image2pipe → VP9 WebM (2–12 fps, default 8, ≤10 min); fallback: in-page `getUserMedia` desktop capture + MediaRecorder → upload to `/v1/recording-upload` | recording | `start_recording`/`stop_recording`/`recording_status` | `betaHome/recordings/brainstem-<iso>.webm` (+`.tmp`) | no | ui-driver-server.test: "walkthrough recording pads with visible recap cards, not dead air", "long recordings stream to disk without base64 IPC"; video-tools.test (ffmpeg resolution) | walkthrough-via-chat (`recording_created`, `recording_decoded`, `five_minute_duration`), drive-via-chat | yes (one recording per window; stop while capture in flight) | `REC AUTOPILOT` indicator; `stop_recording` pads to `minimumDurationMs` with recap chapters per `recapMode`. |
| 52 | Screenshot evidence: PNG to `captures/`, JPEG data URL (≤1280w), visible text of Brainstem frame, artifact URL | evidence | `screenshot`, also appended to `stop_recording` | `betaHome/captures/brainstem-<iso>.png` | no | none direct | show-mode-capture, walkthrough, drive-via-chat, frontier-acceptance | no | Artifact map capped at 100 entries (FIFO). |
| 53 | Artifact serving `GET /v1/{captures,recordings}/<id>?token=` and upload `OPTIONS/POST /v1/recording-upload?token=` (CORS `*`, ≤500 MB, `video/webm` only) | drive bus | HTTP | recordings/ | no | none direct | Surgeon artifact cards load these URLs | no | Separate artifact token from command token. |
| 54 | AI force mode overlay (edge glow + tag), `force_mode on/off/status`, `forceMode:true` on any command, idle auto-off (default 30 s, 1 s–10 min) | force mode | bridge; Surgeon `set_ai_force_mode`; Python driver | DOM `#brainstem-ai-force-mode` | no | show-mode-tour.test: "Brain Surgeon and the Python driver can light AI force mode…" (static) | show-mode-capture `--force` | no | Docs screenshots must be captured with it OFF (README rule). |
| 55 | `route_telemetry` read-through (route manager snapshot + `chat_lease_count` + `navigation_count`) | telemetry | bridge | none | no | log-redaction.test: "route telemetry redacts device codes and credential-bearing previews" | walkthrough-via-chat / walkthrough-gate (heavy use) | no | Snapshot includes up to 500 events. |
| 56 | `refresh` (reload Brainstem frame) | drive | bridge; Surgeon `refresh_brainstem_view` | none | no | none | none | yes (bridge re-install) | — |
| 57 | Long-command keepalive: headers flushed + single-space heartbeat every 15 s | drive bus | all `/v1/command` | none | no | ui-driver-server.test: "long UI commands flush headers and send heartbeats" | — | no | Response body therefore begins with whitespace before JSON. |
| 58 | Stop: abort captured recording, close server (2 s race), destroy sockets, remove owned metadata | drive bus | `driver.stop()` on quit | removes `ui-driver.json` if token+pid match | no | none | none | no | — |

### 1.6 GitHub Copilot Brain Surgeon (`electron/brain-surgeon.mjs`, `copilot-runtime.mjs`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 59 | Copilot runtime: bundled CLI path per platform (`@github/copilot-<os>-<arch>`), stdio connection, 30 s start timeout, reads `~/.brainstem/.copilot_token` (`access_token`), auth status/login, stop/forceStop, token-change restart on `createSession` | copilot | `CopilotRuntime.start/stop/createSession` | reads `.copilot_token`; cwd `brainstemDir` | no (CLI) | copilot-runtime.test (4 tests) | all Surgeon-driven scripts | yes (shared runtime across 12 sessions) | Token change ⇒ runtime restart affects every Surgeon session. |
| 60 | Surgeon session: `createSession` (clientName, enableConfigDiscovery, infiniteSessions, memory, `approveAll` permissions, streaming, system message, tools) | surgeon | `BrainSurgeon.start()` | Copilot SDK session state | yes | brain-surgeon.test: "Brain Surgeon keeps the full Copilot loop and RAPP delegation tools" (fake runtime) | — | yes | `approveAll` = no permission prompts. |
| 61 | `send(prompt)`: one in-flight per session (throws "already working"), `sendAndWait` 1 h, emits `response-start`/`done`/`error` | surgeon | `beta:surgeon-send` | — | yes | brain-surgeon.test (fake runtime) | surgeon-chat.mjs; drive-via-chat; walkthrough | yes | No queueing — renderer also guards `session.running`. |
| 62 | Event mapping: `assistant.message_delta`→delta, `tool.execution_start/complete`→tool cards, `ready`, `reset`, `artifact`, `lease` | surgeon | `session.on` | — | yes | brain-surgeon.test | — | — | — |
| 63 | `reset()` (releases all chat leases, disconnect 3 s race) and `stop()` (+ PAC logins kill) | surgeon | `beta:surgeon-reset`, `beta:surgeon-close`, quit | DOM lease state | no | none | none | yes | — |
| 64 | Multi-Surgeon registry (`Map` by session id, max 12) + shared UI stage: `UI_STAGE_ACTIONS` (announce, chat, click, press, run, start_recording, stop_recording, surgeon_chat, tour, type) serialized through one promise chain; read-only actions pass through | surgeon/concurrency | `ensureBrainSurgeon`, `executeUiCommand` | — | no | surgeon-multichat.test: "preload and main key Brain Surgeon by session id", "the one visible Brainstem is a shared stage…" (static) | none | **yes (core)** | Stage chain keeps moving after a failure. |
| 65 | Tool `delegate_to_brainstem` (prompt, narration, timeout_ms, optional `ephemeral_agent{filename,source}`): with ephemeral → chat lease + `routeManager.withRoute` + wait for `#input` + visible `chat`; cleanup guaranteed | surgeon/route | tool | ephemeral file in live worker `agents/`, manifest edits, `__pycache__` rm | yes | brain-surgeon.test: "failed delegation leaves ephemeral cleanup to the beta route manager", "temporary agent filenames are normalized safely"; route-manager.test (ephemeral suite) | drive-via-chat; walkthrough baseline/repeat-ephemeral/control-handoff; walkthrough-gate | **yes** | `cleanFilename` forces `*_agent.py`, refuses `basic_agent.py`. |
| 66 | Tools `inspect_visible_brainstem`, `capture_visible_brainstem` (image returned to LLM as binary), `clear_brainstem_chat` (clicks "Clear"), `refresh_brainstem_view`, `drive_visible_brainstem` (run steps, force_mode), `drive_twin` (twin_id + steps) | surgeon/drive | tools | captures/ | yes | brain-surgeon.test (tool list); twin-manager.test P3c (static) | walkthrough (drive_visible_brainstem announce) | yes | `drive_visible_brainstem` step enum: announce, click, press, read, type, wait. |
| 67 | Tools `start_demo_recording` / `stop_demo_recording` (minimum_duration_ms, recap_mode enum) → video + image artifacts | surgeon/recording | tools | recordings/, captures/ | yes | brain-surgeon.test (tool list) | walkthrough (all scenarios) | yes | — |
| 68 | Tools `show_rapp_identity`, `list_active_agent_files`, `list_scoped_agents`, `create_agent_stack`, `select_agent_stack`, `remove_agent_stack`, `install_scoped_agent` (fertility-gate refusal returned as `{installed:false, lesson}`), `remove_scoped_agent` | surgeon/stacks | tools | `routing/identity.json`, `stacks/`, `objects/`, `eggs/`; worker swap | yes | brain-surgeon.test: "Brain Surgeon manages nested RAPPID stacks and visibly reloads selection"; route-manager.test | walkthrough stack-churn; walkthrough-gate stack checks | yes | Every mutation calls `startDefault()` (atomic worker swap). |
| 69 | Copilot Studio tools: `ensure_copilot_studio_deploy_agents` (validates/injects bundled Factory ≥1.0.4 & Deploy ≥1.0.14 with `BETA_DRAFT_ONLY = True`; materializes parity cases + industry matrix 0600), `copilot_studio_deployment_defaults` (non-secret), `copilot_studio_auth_status` (`pac auth list/who`), `start_copilot_studio_login` (`pac auth create --deviceCode`), `poll_copilot_studio_login`, `show_copilot_studio_agent_link` (GUID-validated preview URL artifact) | surgeon/CS | tools; Deploy pill | `betaHome/copilot-studio/*.json`; stack install; PAC profiles | yes (+PAC) | brain-surgeon.test: "Brain Surgeon validates and injects bundled Copilot Studio agents", "…emits a clickable Copilot Studio agent link", "deployment agent validation rejects stale filenames"; copilot-studio-auth.test (3); copilot-studio-resource.test (2) | none | yes | Secrets only reported as `secret_present`. |
| 70 | Twin tools: `list_rapplications`, `hatch_rapplication(store_id, instruction)`, `loop_brainstem_with_twin(twin_id, goal)`, `deploy_to_copilot_studio(display_name, environment, agents[])`, `open_auth_window(url|id, note)` | surgeon/twins | tools | twins/, network | yes | twin-manager.test: "main wires twins + store IPC and the Surgeon hatch tools", "…Copilot Studio deploy twin…", "…two-brain loop", "away-auth…" (all static) | frontier-acceptance.mjs (hatch `json_doctor` + loop) | yes | `json_doctor` id only exists in the Public RAR store, not the default AIBAST source. |
| 71 | Tools `set_ai_force_mode`, `show_mode_click_through`, `check_beta_updates` | surgeon/misc | tools | — | yes | brain-surgeon.test (tool list); show-mode-tour.test | walkthrough Chapter 5 (`check_beta_updates`) | yes | — |
| 72 | System message (teaching posture, golden path, force-mode rule, twins handoff, Show Mode) | surgeon | `SURGEON_SYSTEM_MESSAGE` | — | yes | brain-surgeon.test exports `systemMessage` (content not asserted beyond presence) | — | — | Model behavior is prompt-driven; non-deterministic. |

### 1.7 Route manager — identity, stacks, composition, workers (`electron/route-manager.mjs`, `brainstem-process.mjs`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 73 | Brainstem config resolution (`BRAINSTEM_HOME`, `BRAINSTEM_BETA_SOURCE_DIR`, `BRAINSTEM_BETA_PYTHON`, `BRAINSTEM_BETA_PORT`/`PORT` default 7071) | process | `resolveBrainstemConfig` | — | no | brainstem-process.test: "beta launcher resolves the shared global Brainstem", "…Windows Brainstem paths on every host" | — | no | Log file `~/.brainstem/logs/beta-brainstem.log` (default config; workers override). |
| 74 | `BrainstemProcess.start`: reuse an already-healthy server on the URL (`owned=false`), else spawn `python brainstem.py` with `PORT`, `BRAINSTEM_BETA_LAUNCHER=1`, `PYTHONUTF8=1`, piped stdout/stderr through redaction into a 0600 log; wait for `/health` up to 90 s; `stop` SIGTERM→SIGKILL + bounded log flush (5 s) | process | route manager / twins | `logs/workers/<hash>.log`, `logs/twins/<id>.log` | no | brainstem-process.test: "accepts authenticated and unauthenticated health", "health wait stops when the child exits", "health wait returns the first valid response"; log-redaction.test: "BrainstemProcess redacts both fake-kernel streams without burst loss", "log shutdown is bounded when an inherited pipe never reaches EOF" | all live scripts | yes | Health = `{status: ok|unauthenticated, version, agents[]}`. Reuse means a stray server on the chosen port is silently adopted. |
| 75 | Identity mint (`routing/identity.json`: caller RAPPID, memory GUID = UUID anchor, default stack) + default stack JSON; back-fills `active_stack_rappid`/`overlay_stack_rappids` | identity | `identity()` | `routing/identity.json`, `routing/stacks/<sha256(rappid)>.json` | no | route-manager.test (all); rapp-protocol.test: "keyless identity reuses the UUID memory anchor" | walkthrough Chapter 1 | no | Owner from `BRAINSTEM_BETA_RAPP_OWNER` (default `microsoft`). |
| 76 | Stacks: `createStack` (slug name, parent default = active), `selectStack` (leaf + ordered unique overlays, leaf≠overlay), `removeStack` (refuses default/active/overlaid/non-empty/parent), `stackTree` (cycle detection), `stackLineage`, legacy `normalizeStack` | stacks | Surgeon tools | `routing/stacks/*.json`, `identity.json` | no | route-manager.test: "nested stacks inherit agents, preserve overlay order, and share memory", "persistent stack churn can return the tree to its exact baseline", "legacy stack manifests acquire a deterministic display name", "inherited and overlay filename collisions fail closed" | walkthrough stack-churn + gate | yes (lock) | Telemetry: stack-created/selected/removed. |
| 77 | Scoped agent install with **fertility gate** (candidate composition dry-loaded against the Grail kernel in a temp dir BEFORE persisting), `packageAgent` (content-addressed object + rapp/1 egg, mint-once RAPPID reuse), `removeScopedAgent`, `listScopedAgents` | agents | Surgeon tools; store `⇣ Install` | `routing/objects/<Hb>.py`, `routing/eggs/<Hb>.egg`, stack JSON; temp `.install-gate-*` | no (python subprocess) | molt-lineage-integration.test: "the install gate refuses a scoped agent that fails the Grail dry-load"; rapp-store.test: "store installs validate and persist before an atomic routed-worker swap" (static); route-manager.test | walkthrough stack-churn | yes | Agent source 1 B–512 KiB; filename `*_agent.py`, never `basic_agent.py`. |
| 78 | Global agent enumeration from `brainstemDir/agents` (+ generated `RoutedContextMemoryAgent`/`RoutedManageMemoryAgent` bound to memory GUID, scope `memory`) | composition | `globalAgentEntries` | reads user's `~/.brainstem/src/rapp_brainstem/agents/*_agent.py` | no | route-manager.test: "global source revisions create a new composition hash" | — | no | Memory agents cannot be deleted. |
| 79 | `compositionDescriptor`: global + inherited + leaf + overlays + optional ephemeral (nonce), lineage overlay, collision fail-closed, canonical sha256 composition hash | composition | everywhere | — | no | route-manager.test: "route compositions combine global and scoped agents without byte copies", "ephemeral agents participate in one composition and collisions fail closed" | — | no | — |
| 80 | Materialization: staging dir → hardlink-or-copy objects / write ephemeral+molt bytes → whole-set dry-load in a private copy → `complete.json` → atomic rename; byte-match assertions before/after; negative cache (60 s TTL) per hash; refuses to replace an invalid composition while its worker is live | composition | `materializeCompositionOnce` | `routing/compositions/<hash>/{agents,complete.json}`, `.dry-load-*`, `.<hash>.*.stage` | no (python) | molt-lineage-integration.test: "HARD 6 — composition validates in staging before one atomic publication", "dry-load executes private copies and cannot rewrite publishable bytes", "a known-bad composition is not re-validated on every request" | — | yes | Dry-load script imports the real `brainstem.py` (`_load_agent_from_file`, `_quarantine_snapshot`). |
| 81 | Fallback ladder on materialization failure: last-good (same route identity) → pristine baseline → per-locus isolation (accept/reject rings, move HEADs) → **quarantine** (re-compose without files Grail named; baseline/memory files protected) | resilience | `materializeComposition` | `lineageRoot` HEADs; compositions | no (python) | molt-lineage-integration.test: "HARD 4…", "fail-safe prefers the last-good parent ring…", "fail-safe isolates a broken locus…", "an unrelated invalid scoped agent does not reset healthy lineage HEADs", "boot with a persisted broken scoped agent and no last-good quarantines and boots", "real Grail boot quarantines a persisted broken scoped agent and boots", "an explicit stack switch never resurrects the previous stack's last-good composition", "a colliding user agent never evicts the pristine baseline agent" | — | yes | Telemetry `composition-quarantine`, `lineage-composition-fallback`. |
| 82 | Worker lifecycle: `startWorker` (allocate loopback port, `AGENTS_PATH`, `BRAINSTEM_BETA_ROUTED_WORKER=1`), `activate` (onActivate → `state.url` → iframe src), `retireInactiveWorker` (waits for idle ≤30 s), `stop` | workers | `startDefault` | `logs/workers/<hash>.log`; processes | no | route-manager.test: "ordinary route changes retire the superseded worker", "route replacement waits for ephemeral cleanup before stopping old worker", "ephemeral request queued behind replacement uses the new active route" | walkthrough-gate: "worker churn is balanced and returns to one worker" | **yes** | Workers keyed by effective composition hash. |
| 83 | Route locks (`__lifecycle__` + per-composition) with lock telemetry; ephemeral requests on the same route serialize | concurrency | `withRouteLock` | — | no | route-manager.test: "same-route ephemeral requests serialize without overlap" | walkthrough-gate: "telemetry proves complete lock-to-cleanup ordering" | **yes** | — |
| 84 | Ephemeral agent on the LIVE worker (`withActiveEphemeral`): write file into worker `agents/`, patch `complete.json` (`active_composition_hash`), transient composition hash on route, callback, then rm file + `__pycache__` + manifest restore; or new worker path + `retireEphemeralWorker` re-key | ephemeral | `withRoute({ephemeralAgent})` | worker `agents/`, `complete.json` | no | route-manager.test: "ephemeral cleanup preserves the live route while removing the tool", "consecutive ephemeral turns reuse the live worker and clean up each time"; molt-lineage-integration.test: "ephemeral + lineage fallback teardown retires the effective worker and its composition" | walkthrough repeat-ephemeral (+gate: exactly two injections/cleanups, same worker URL, request-unique hashes) | **yes** | Telemetry: ephemeral-injected / -callback-start / -end / -error / -cleaned. |
| 85 | `removeActiveAgent` (global rm from the user's agents dir; stack remove; refuses memory/ephemeral) / `readActiveAgent` / `activeAgentFiles` | agents | IPC delete/read/list | `brainstemDir/agents` | no | route-manager.test: "active-agent deletion persists for stack and global sources" | — | yes | — |
| 86 | Telemetry ring (500 events, sequence numbers) + `telemetrySnapshot` (active route, worker/stack counts, stack tree, composition fingerprint, object/egg counts); details pass through `redactSensitiveValue` | telemetry | `recordTelemetry` | — | no | route-manager.test: "persistent route callback telemetry binds request ID and proof response"; log-redaction.test (telemetry) | walkthrough-gate (dozens of telemetry assertions) | no | — |
| 87 | `materializeExternalAgentSet` for twins: lineage overlay per file, staging + dry-load, refuse to mutate an existing published `AGENTS_PATH`, twin-baseline fallback | twins/composition | TwinManager | `twins/<id>/agents` | no (python) | molt-lineage-integration.test: "independent twin AGENTS_PATH composition receives the same verified overlay", "a twin-specific overlay failure falls back locally without moving shared HEAD" | — | yes | — |

### 1.8 Molt Lineage (`electron/lineage-store.mjs`, `lineage-control.mjs`, `molt-lineage.mjs`, `rings/context_memory_agent.ring1.py`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 88 | LineageStore: baseline ancestors from `brainstemDir/agents`, `appendRing` (hash-chained, staging rename, verified flag upgrade), `HEAD`/`PRIOR_HEAD`, `resolveRing`/`resolveLive` (fail-safe to baseline), `verifyChain`, `inspectLineage` (corrupt ring count), CRLF→LF canonical identity, Windows MAX_PATH-safe segments, legacy content-keyed locus migration | lineage | route manager | `lineageRoot/<seg>/{locus.json,HEAD,PRIOR_HEAD,rings/<seg>/{source.py,meta.json}}`, `.migrated-*` | no | lineage-store.test (15 tests); molt-lineage.test (10); molt-lineage-integration.test | none | yes (two workers sharing a lineage root) | Ancestor id derived from filename only (survives Grail upgrades). |
| 89 | Per-locus policy `pinned`/`mutable` (`setLocusPolicy`; pinned forces HEAD=baseline) | lineage | store API (no UI) | `locus.json` | no | lineage-store.test: "a pinned locus never molts while its siblings molt freely" | none | no | No Surgeon tool or chat word sets policy. |
| 90 | Safe words: `baseline` (rollback all loci, records PRIOR_HEAD) / `restore` (prior head, else newest verified; honors pin), env `RAPP_BASELINE_SAFEWORD`, `RAPP_RESTORE_WORD`; honest replies (disabled / partial / nothing-to-restore / kept last-good) | lineage UX | chat message exact match (bridge → IPC) | HEADs; restarts route | no | lineage-control.test (9); lineage-store.test: "restore returns to where you were…", "a restore that recovers nothing is not reported as a restore" | none | yes | Reply rendered into the Grail chat as an assistant bubble. |
| 91 | Kill switch `RAPP_MOLT_LINEAGE=0` (gates reads AND writes; `DISABLED_REPLY`) | lineage | env | — | no | molt-lineage-integration.test: "HARD 5…"; lineage-store.test: "kill switch gates HEAD WRITES too…" | none | no | — |
| 92 | Seeded ContextMemory ring 1 (Ambient Context: `<system_status>` broken-agent scan + `<operating_context>`), sha-pinned to a specific Grail baseline (`3f9ba4ec…`), verified by the Molter gate (python subprocess with `utils.azure_file_storage` shim) | lineage/ambient | constructor `seedContextMemoryRing1` | `lineageRoot` ring + HEAD; tmp `rapp-molt-verify-*` | no (python) | molt-lineage-integration.test: "HARD 1/2/3…", "a CRLF ring source (Windows autocrlf checkout) still composes ring 1", "packaged Frontier exposes the Molter gate outside app.asar"; frontier-rapplication-hardening.test (molter verifier suite) | none | no | Skipped (with telemetry) if baseline bytes differ from the pin — silently runs ring 0 on other Grail versions. |
| 93 | Lineage overlay at composition time (`resolveLineageEntry`; generated memory agent gets `routedContextMemoryMoltSource`; size cap 512 KiB) | lineage | composition | — | no | molt-lineage-integration.test | none | no | Telemetry `lineage-overlay-skipped` on error. |
| 94 | Enterprise ALM library `MoltLineageStore` (env-scoped HEADs, fast-forward `promote` with named CONFLICT, hash-chained `promotions.json`, `detectDrift`, `walk`) — library only, not wired to UI | lineage/ALM | module import | `lineageRoot/<hex>/{ancestor.json,rings/*.json,heads/<env>.json,promotions.json}`, `filenames.json` | no | molt-lineage.test (10) | none | no | Different on-disk layout from LineageStore; identity unified. |

### 1.9 RAPPlication twins (`electron/twin-manager.mjs` + main.mjs twin helpers)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 95 | Hatch from store (sha-verified cartridge; optional egg/UI; provenance note distinguishes default vs custom catalog) | twins | store picker `◈ Hatch`; `beta:twin-hatch`; Surgeon `hatch_rapplication` | `betaHome/twins/<id>/{agents/,<id>.egg}`; `logs/twins/<id>.log`; new process+port | no (hatch) / yes (instruction loop) | twin-manager.test: "store hatch refuses a traversal filename…", "a twin is driven only over /chat…" (static); rapp-store.test: "a download carries which catalog vouched for it" | frontier-acceptance (Public RAR `json_doctor`) | yes (maxTwins 8) | Twins root wiped on every app start (orphan processes not reaped). |
| 96 | Hatch from dropped `.egg` (fail-closed `verifyEgg`, variant `rapplication`, `agents/*_agent.py` → AGENTS_PATH, `ui.html` → custom UI, others → resources, duplicate-basename refusal) | twins/eggs | drop; `beta:twin-hatch-egg` | twins/<id> | no | frontier-rapplication-hardening.test: "a grown capability survives the launcher clearing the twins root"; rapp-protocol.test (egg vectors) | none | yes | Nothing fetched. |
| 97 | `hatchLocal` — Frontier-owned twin from bundled sources (Copilot Studio deploy twin: Factory + Deploy agents + parity cases + industry matrix + scripted instruction, Draft-only) | twins/CS | `beta:twin-deploy-copilot-studio`; Surgeon `deploy_to_copilot_studio` | twins/<id> incl. resource files | yes (loop) + PAC | twin-manager.test: "the Copilot Studio deploy twin is composed from the bundled Factory + Deploy agents (P2)" (static); copilot-studio-resource.test | none | yes | Id base `copilot-studio-deploy`; includes selected active business agents. |
| 98 | Worker composition per twin: safe `*_agent.py` basenames, duplicate refusal, port allocation, env `AGENTS_PATH`, `BRAINSTEM_BETA_ROUTED_WORKER=1`, `BRAINSTEM_BETA_TWIN=<id>`, **`BRAINSTEM_LAN_MODE=0`**, RAPPID mint from first agent | twins | `#hatchComposed` | process, logs | no | twin-loop-hardening.test: "twin workers are pinned to loopback regardless of ambient LAN mode (canon)" (static); twin-manager.test: "twinSlug…", "TwinManager requires a store client and brainstem config" | — | yes | Events: twin-hatched, twin-status, twin-log, twin-message, twin-closed, twin-needs-auth. |
| 99 | Twin chat (`POST <twin>/chat` with per-room `conversation_history` ≤40 sent / 80 kept; author labels; error events) | twins/chat | tile composer; `beta:twin-chat`; Surgeon via loop | — | yes | twin-loop-hardening.test: "chat() forwards per-room conversation memory to the twin" | frontier-acceptance (indirect) | yes (room shared by user/loop/Surgeon) | Kernel `/chat` is stateless; history is the only continuity. |
| 100 | `run(id, instruction)` bounded loop (maxRounds 4; `saysDone` negation-aware; `NEEDS_AUTH_RE` pause → `needs-auth`; empty reply = error) | twins/loop | `beta:twin-run` (no UI caller); hatch with instruction | — | yes | twin-loop-hardening.test: "the needs-auth gate matches an auth REQUEST…", "run(): an empty twin reply is treated as an error…" | none | yes | IPC `beta:twin-run`/preload `twinRun` exist but renderer never calls them. |
| 101 | `loop(id, goal)` two-brain loop: visible Brainstem plans via its own `/chat` (session `twin-loop-<id>`, planner history), twin executes, `DONE —` detection, maxRounds 6 with “Paused after…” message, empty-goal error surfaced | twins/loop | `⟳ Loop` button; `beta:twin-loop`; Surgeon `loop_brainstem_with_twin` | — | **yes (two model chains)** | twin-loop-hardening.test: "loop(): the Brainstem keeps the goal + prior turns after round 1", "loop(): an empty goal surfaces a visible error…"; twin-manager.test (static) | frontier-acceptance.mjs | **yes** | Planner chatter goes to the active routed Brainstem (`state.url`), not the user's session. |
| 102 | `close(id)` (sets `closed`, stops worker, rm dir, always emits `twin-closed`) / `stopAll` | twins | tile `×`; `beta:twin-close`; quit | twins/<id> removed | no | twin-loop-hardening.test: "close() signals an in-flight loop…" | none | yes | — |
| 103 | Away-auth: `twin-needs-auth` → OS `Notification`; click focuses window, sends `beta:twin-focus`, opens parsed URL (default `https://microsoft.com/devicelogin`) in user's browser; `beta:open-auth` / tile `Sign in to continue →` / Surgeon `open_auth_window` | twins/auth | events | none (no credential capture) | no | twin-manager.test: "away-auth: notification + browser pop-out…" (static) | none | no | `parseAuthPrompt` extracts URL + `XXXX-XXXX` code; `twin-event` payloads pass through `redactSensitiveValue` (codes redacted in renderer copy). |
| 104 | Pop-out window `⤢ App` (1180×860 full or 460×860 mobile per `preferredView`; sandboxed child BrowserWindow; loads `<twin>/?beta=1` then `document.write`s the custom UI + force-mode bootstrap + host view toggle `#__rappViewToggle`) | twins/UI | tile button; `beta:twin-popout` | none | no | twin-manager.test P3 (static) | none | yes (second window) | Twin with no custom UI shows Grail chat. |
| 105 | In-tile UI injection (`beta:twin-inject-ui` → `findTwinFrame` → `document.write` custom UI; WeakSet guard) + `instrumentRappUi` (`window.__rappForceModeCapable`, `data-rapp-force-mode="ready"`) | twins/UI | IPC only (renderer does not call it today) | none | no | none | none | no | Design doc notes inline injection was abandoned for the chat-log + pop-out split. |
| 106 | Concurrency caps: max 8 twins, max 12 Surgeons; herd shares one visible Brainstem stage | limits | errors surfaced in herd banner | — | no | none (caps asserted only by messages) | none | yes | — |

### 1.10 RAPP Store / RAR library (`electron/rapp-store.mjs`, `frontier/`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 107 | Store sources: `aibast` (default, AIBAST `registry.json`), `frontier` (`beta/frontier/store/index.json`), `public` (`kody-w.github.io/RAPP_Store`), `custom` (https or exact loopback http); persisted `store-source.json`; facade follows the toggle for picker AND twins | store | `beta:store-source`; picker tabs | `betaHome/store-source.json` (0600) | no | rapp-store.test: "store source URLs accept only https or an exact loopback HTTP host" | none | no | — |
| 108 | Catalog load: `rapp-store/1.0` shape or AIBAST-style `agents[]` adapter (raw-base only for the canonical Microsoft Pages URL; others relative); cached per client | store | `list()` | network | no | rapp-store.test: "loads the catalog and flags gated entries", "adapts an AIBAST-style registry into pinned store entries", "registry adapter resolves raw URLs relative to a custom catalog base", "rejects an unexpected catalog schema" | none | no | 15 s timeout. |
| 109 | `resolve`: yanked / deprecated / `min_app_version` (vs launcher `package.json`) refusals with codes | store | — | — | no | rapp-store.test (4 tests incl. "a yanked registry agent is also refused by the normalized client", "the enforced app version defaults to the launcher's own package.json") | none | no | — |
| 110 | `download`: sha256 fail-closed (no pin = refuse), gated 404 → auth-needed error, egg pin, UI pin (unpinned never fetched; fetch failure degrades to Grail chat), provenance `trustedSource` | store | hatch/install | — | no | rapp-store.test (sha mismatch, gated, egg, UI, provenance); twin-loop-hardening.test: "the RAPP Store fails CLOSED when a singleton has no sha256 pin" | none | no | — |
| 111 | `⇣ Install` into the running Brainstem = store download → scoped install (fertility gate) → `startDefault` swap | store/agents | `beta:store-install-agent` | stack JSON, objects, eggs; worker swap | no (python) | rapp-store.test: "store installs validate and persist before an atomic routed-worker swap" (static) | none | yes | “ready on your next message” banner. |
| 112 | Frontier store build (`frontier/build_store.py` → `store/index.json`, pins computed from disk, refuses missing files) and egg fledging (`frontier/build_rapp_egg.mjs` → `<name>.egg` with `agents/*`, `ui.html`, `soul.md`) | store tooling | CLI | `frontier/store/index.json`; egg file | no | none (CI diff gate mentioned in file comments) | none | no | Five entries: molter, toaster, ui-smith, agentic-app-studio, agent-migration. |
| 113 | Frontier rapplication **Molter** (`search_capability`, `acquire`, `mutate`, `generate`, `rollback`, `molt_log`, `status`; AST+isolated verify gate; installs only when `BRAINSTEM_BETA_TWIN` matches its real agents dir) | rapplication | twin chat | `~/.rapp/molter/**`; twin `agents/` | yes (via twin chat) | frontier-rapplication-hardening.test (molter verifier ×6, "molter installs live only in the marked twin's real agents directory", "the gate refuses a molt that would kill a plain Grail brainstem on import", "every Frontier agent stays safe to drag into a plain Grail brainstem") | none | no | Fetches AIBAST registry over network. |
| 114 | **Toaster** (`toast`, `export_skill`, `untoast`, `roundtrip_check`) | rapplication | twin chat | `~/.rapp/toaster/**` | yes | frontier-rapplication-hardening.test (toaster ×4) | none | no | — |
| 115 | **UI Smith** (`generate_ui`, `molt_ui`, `rollback`, `ui_log`, `status`) | rapplication | twin chat | `~/.rapp/ui-smith/**` | yes | frontier-rapplication-hardening.test (UI Smith ×2) | none | no | — |
| 116 | **Agentic App Studio** (`list_agents`, `add_agent`, `design_app`, `serve`, `stop_server`, `deploy`, `status`; generates Power Apps code app + `serve.py` local server with same-origin `/chat` proxy; `pac` deploy) + its `index.html` UI (AgenticDrive bus `#driveText/#driveSend/#driveLog`) | rapplication | twin chat / custom UI | `~/.rapp/agentic-app-studio/**`, spawned `serve.py` process, PAC | yes | frontier-rapplication-hardening.test (scaffold ×3, RAR URL refusal, filename disambiguation) | none | yes (child server process) | Only entry with `ui_sha256` alongside agent-migration. |
| 117 | **Agent Migration Assistant** (`analyze`, `convert`, `status`, `report`) + `index.html` UI (`#payload/#analyze/#convert/#status/#out`) | rapplication | twin chat / custom UI | `~/.rapp/agent-migration/**` | yes | frontier-rapplication-hardening.test: "migration scaffolds encode hostile client strings as inert Python literals" | none | no | — |
| 118 | Bundled Copilot Studio agents (`resources/copilot-studio/`): `RappCopilotStudioFactoryBeta` 1.0.4 (doctor, plan, build, extend, provision, parity, finalize, verify, release_plan, status) and `CopilotStudioDeployBeta` 1.0.14 (doctor, plan, deploy, provision, parity, push, finalize, release_plan, sync_plugin), `BETA_DRAFT_ONLY = True`; parity cases + industry matrix JSON | CS resources | Surgeon ensure tool / deploy twin | stack install | yes + PAC + tenant | copilot-studio-resource.test (2); brain-surgeon.test | none | no | 253 KB deploy agent; never runs in CI against a tenant. |

### 1.11 Updates & release train (`electron/update-manager.mjs`, `update-runner.mjs`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 119 | Check for updates: verify checkout layout, origin/`update-config.json`/env remote, validated ref (default `main`), dirty check, shallow fetch branch + `beta/VERSION`, resolve **annotated** `brainstem-beta-v<ver>` tag and its `beta/VERSION`, prerelease-aware compare, `channelBehind`, `sameVersion` re-align | updates | menu, in-frame button, `beta:check-for-updates`, Surgeon `check_beta_updates` | git objects in `betaHome/src`; `state.update` | no (git/network) | update-manager.test (17 tests) | drive-via-chat (clicks check, reads status); walkthrough Chapter 5; walkthrough-gate: "update check completed without an error state" | yes (`updateCheckInFlight` guard) | Phases: checking/current/available/blocked/error. |
| 120 | Install update: managed-install checks (portable Node, Electron), both checkouts clean & unchanged, stage pinned installer + **rollback installer** + runner + request JSON in `betaHome/cache`, spawn detached node runner, `app.quit()` | updates | `beta:install-update`, in-frame **Update and Restart** | `betaHome/cache/*`, `update-result.json` removed, `update.log` | no | update-manager.test: "managed installs resolve the portable Node and Electron runtimes" | none (scripts forbid clicking Update and Restart) | yes | Windows adds `bootstrapUrl`/`rollbackBootstrapUrl` (install.ps1 raw). |
| 121 | Update runner: wait for parent exit (≤120 s), assert checkouts unchanged, run installer with `BRAINSTEM_BETA_NO_LAUNCH=1`, on failure run rollback installer and verify HEAD, write result, clean temp files, relaunch Electron | updates | detached process | `update-result.json`, `update.log`, both checkouts | no | update-runner.test (6 tests, POSIX only) | none | no | Pre-flight refusal ⇒ no rollback. |
| 122 | Startup consumption of `update-result.json` → success / error (+rollback detail, log path, guidance) into `state.update` | updates | `loadPendingUpdateResult` | deletes result file | no | none | none | no | — |
| 123 | Release bootstraps `frontier.sh` / `frontier.ps1`: resolve newest `brainstem-beta-v*` GitHub release → 40-char commit → run commit-pinned `beta/install.sh`/`install.cmd` with `BRAINSTEM_BETA_RELEASE_TAG`, `_COMMIT`, `_RUNTIME_VERSION_URL`; `RAPP_FRONTIER_REPO`, `RAPP_FRONTIER_RESOLVE_ONLY=1` | install | one-liners on the landing page | `~/.brainstem/**` | no | installer-contract.test: "stable Frontier bootstraps resolve and run the latest published release", "dedicated beta page resolves fork releases without changing main install", "dedicated beta page scripts parse" | none | no | GitHub API + raw.githubusercontent. |
| 124 | `install.sh`: sparse partial clone (`beta`, `tools/rapp1`), global Brainstem install (release tag or commit; fork URL via `insteadOf`), portable Node 24.19.0 with SHASUMS verification, `npm ci --ignore-scripts` + explicit Electron install, `npm run check`, `npm test` (with `BRAINSTEM_BETA_RUNTIME_DIR`), writes `update-config.json`, `launch.sh`, `surgeon-chat.sh`, `brainstem-chat.sh`, `brainstem-walkthrough.sh`, `~/.local/bin/brainstem-{frontier,beta,surgeon,chat,walkthrough}`, `.app` bundle or `.desktop`, optional background launch | install | curl | `betaHome/**`, `~/.brainstem/**`, `~/.local/bin`, `~/Applications`, `~/.local/share/applications` | no | installer-contract.test (17 tests, static/regex); video-tools.test: "the factory install never runs package lifecycle scripts" | none (manual per RELEASING §5) | no | `BRAINSTEM_BETA_NO_LAUNCH=1` for CI. |
| 125 | `install.cmd` (Windows twin of install.sh; PowerShell bootstrap; `create-windows-shortcuts.js` Desktop + Programs `.lnk`) | install | PowerShell one-liner | `%USERPROFILE%\.brainstem\beta-launcher\**`, shortcuts | no | installer-contract.test (static) | none | no | Legacy "RAPP Brainstem Beta.lnk" removed. |
| 126 | Landing page `beta/index.html` (resolves release metadata client-side, commit-pinned copy commands, fork-aware `RAPP_FRONTIER_REPO`) | docs/install | GitHub Pages `/beta/` | none | no | installer-contract.test: "dedicated beta page…" | none | no | — |
| 127 | `package-gate.mjs` (packaged mac app exists/newer than source, ffmpeg/ffprobe unpacked+executable+arm64, headless isolated smoke) and `npm run dist:mac` | release | CLI | tmp `rapp-beta-package-gate-*` | no | none (it is itself a gate); walkthrough-certify runs it | — | no | macOS arm64 only. |
| 128 | RELEASING.md procedure (annotated component tag, installer sha256s, source-only prerelease, clean-HOME install test) | release | manual | — | no | update-manager.test: "release tags are component-qualified (RELEASING.md §2)" | none | no | — |

### 1.12 Drivers / external AI entry points (`scripts/`)

| # | Feature | Area | Entry points | State touched | Model? | Existing tests | UI-driven coverage | Concurrency | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 129 | `brainstem-chat.mjs` (`brainstem-chat "<msg>"`): launches Frontier if bridge missing, visible `chat` action, 5 min timeout | driver | CLI | — | **yes** | none | itself | no | Authenticates via `ui-driver.json` token. |
| 130 | `surgeon-chat.mjs` (`brainstem-surgeon "<task>"`): visible `surgeon_chat`, 1 h timeout | driver | CLI | — | **yes** | none | itself | no | — |
| 131 | `drive-via-chat.mjs` (`npm run drive:e2e`): asks Surgeon to hotload `brainstem_ui_driver_agent.py` (renamed per pid) as ephemeral agent and drive the update dropdown with recording; marker `BETA_UI_E2E_COMPLETE` | driver/e2e | CLI | recordings/, captures/ | **yes (Surgeon + Brainstem)** | none | itself | no | Never clicks Update and Restart. |
| 132 | `brainstem_ui_driver_agent.py` (`BrainstemUiDriver`): Python agent calling the bridge with snake_case→camelCase params; actions incl. tour/force_mode; structured `captures`/`recordings` results | driver/agent | hotloaded via Surgeon or dropped into agents/ | — | yes (when invoked by kernel) | installer-contract.test: "chat can hot-load an animated driver for the real frontend"; show-mode-tour.test (static) | drive-via-chat | no | Reads `ui-driver.json` inside the worker (env `BRAINSTEM_BETA_UI_DRIVER_FILE`/`BRAINSTEM_HOME`). |
| 133 | `walkthrough-via-chat.mjs` (`brainstem-walkthrough`, scenarios `baseline` / `repeat-ephemeral` / `stack-churn` / `control-handoff`, `--ingest-existing`): six-chapter Surgeon prompt, pre-steps (enter, logo, telemetry), direct Brainstem turns for handoff, post-checks, ffprobe/ffmpeg 5-frame review, report JSON + gallery `walkthroughs/index.html` | driver/e2e | CLI / npm scripts | `walkthroughs/{runs,reviews,index.html}`, recordings | **yes** | walkthrough-control-handoff.test: "walkthrough includes a measured direct-to-Surgeon control handoff" (pure function) | itself | yes | 20 min default timeout. |
| 134 | `walkthrough-gate.mjs`: ~60 requirements over the latest scored run (duration 270–420 s, decoded, resolution, hashes, freshness ≤6 h, provenance ↔ live app pid/fingerprints, scenario telemetry proofs, kernel diff clean, package gate) | gate | CLI (`--allow-uncertified`) | reads betaHome | no (reads evidence) | none | — | no | Requires live bridge + prior walkthrough. |
| 135 | `walkthrough-certify.mjs`: runs `npm run check`, `npm test`, `package:gate`, `walkthrough-gate --allow-uncertified`; writes `validations/<run>/` logs and stamps `report.validation` | gate | CLI | `walkthroughs/validations/**`, run JSON | no | none | — | no | — |
| 136 | `frontier-acceptance.mjs`: live force-mode acceptance (shell healthy, Surgeon hatch `json_doctor` + `loop_brainstem_with_twin`, transcript shows BRAINSTEM → JSON DOCTOR → DONE) | acceptance | CLI | twins | **yes** | none | itself | yes | Depends on the Public RAR entry `json_doctor`. |
| 137 | `npm run check` (node --check over 29 files) and `npm test` (`node --test`, 28 files) | quality | CLI / install.sh | — | no | — | — | no | No linter/formatter. |

### 1.13 Documented invariants (must not regress)

| # | Invariant | Where stated | Enforced by | Notes |
|---|---|---|---|---|
| 138 | Grail kernel unchanged; no beta routing fields/endpoints in `brainstem.py`; chat is the only wire | GOLDEN_PATH, CONSTITUTION Art. I/IV, twins DESIGN/COMPLIANCE | molt-lineage-integration.test: "HARD 2 — Grail remains blind to Molt Lineage"; walkthrough-gate: "Grail kernel has no beta diff"; twin-manager.test: "a twin is driven only over /chat" | — |
| 139 | Factory install = Grail + launcher + twins + Show Mode; sacred three agents (ManageMemory, ContextMemory, HackerNews); JIT capability acquisition | CONSTITUTION Art. I | none (no test asserts the agents dir contents) | — |
| 140 | Every produced agent must survive import in plain Grail (no module-level exit/hang) | CONSTITUTION Art. I | frontier-rapplication-hardening.test ("…safe to drag into a plain Grail brainstem", "…never emits an agent that would kill a Grail brainstem") | — |
| 141 | Molting only inside isolated twins; sacred agents dir never mutated by generation | CONSTITUTION Art. I | frontier-rapplication-hardening.test: "molter installs live only in the marked twin's real agents directory" | — |
| 142 | Organs/senses opt-in, installed on first enable, removable; capture per-window only; Showtime off by default | CONSTITUTION Art. II/III | `.npmrc ignore-scripts`; video-tools.test: "media tooling degrades to the system binary…", "the factory install never runs package lifecycle scripts" | — |
| 143 | Eggs verified fail-closed; local eggs never uploaded; twin device stores never leave the machine | CONSTITUTION Art. IV | rapp-protocol.test; hatchEgg path | — |
| 144 | Twin workers loopback-only; store downloads sha-verified; gated entries honored; Draft-only Copilot Studio; no secrets in chat/logs | twins DESIGN/COMPLIANCE, LOG-REDACTION | twin-loop-hardening.test; rapp-store.test; log-redaction.test; copilot-studio-auth.test: "deployment defaults return no client secret" | — |
| 145 | Brandmark never redrawn; style tokens; blue = Brainstem, purple = twin; force mode off in doc screenshots | STYLE-GUIDE | brandmark.test; style-guide.test (3) | — |
| 146 | RAPP/1 conformance (canonical JSON, H/Hb, rappid, frames, eggs) pinned to `SOURCE_COMMIT d2cd5abe…` with `tools/rapp1/conformance-vectors.json` | rapp-protocol.mjs, COMPLIANCE (pin lag noted) | rapp-protocol.test (6) | Vectors live outside `beta/` (sparse checkout includes `tools/rapp1`). |
| 147 | Ambient Context laws (implicit, additive, no kernel change, labeled trust, AST-only scanners) | AMBIENT-CONTEXT-PROTOCOL | molt-lineage-integration.test HARD 3 (memory tail byte-identical) | ring-1 python itself has no dedicated test in beta/. |
| 148 | UI Autosteer (`rapp-ui-autosteer/1.0`): scan → plan → inject (copy/frame only, banner comment) → AgenticDrive postMessage bus → evidence | UI-AUTOSTEER-PROTOCOL, CONSTITUTION Art. V | frontier-rapplication-hardening.test (studio scaffolds) | Host tier C = `instrumentRappUi`. |
| 149 | `rapp-force-mode/1.0` marker on every rendered rapplication frame (proposed iframe API) | docs/twins/RAPP-FORCE-MODE.md | none | invoke/result routing not implemented. |
| 150 | Log/export redaction: kernel stdout/stderr treated as credential-bearing; 0700/0600 log modes; JSON Blob exports scrubbed; telemetry + twin logs scrubbed | docs/LOG-REDACTION.md | log-redaction.test (7) | NEW (81337225). |

---

## 2. Complete `/drive` (UI driver bus) action catalog

Transport: `POST http://127.0.0.1:<port>/v1/command`, header `Authorization: Bearer <token>`, JSON body ≤ 256 KiB. `port`/`token` come from `betaHome/ui-driver.json` (`{version, host, port, token, pid, brainstemRuntimeFingerprint, runtimeFingerprint, startedAt}`). Response: `200 {ok:true, result}` (preceded by heartbeat spaces for long commands) or `400/401/404/503 {ok:false, error}`. Common top-level fields on any command: `target:"shell"` (drive the Electron shell frame instead of the Brainstem iframe), `twin:"<id>"` (drive that twin's iframe in the herd), `forceMode:true` + `forceLabel` (light force mode first), `idleMs` (re-arm force-mode auto-off).

Artifacts: `GET /v1/captures/<id>?token=<artifactToken>`, `GET /v1/recordings/<id>?token=…` (≤100 live entries). Upload: `OPTIONS|POST /v1/recording-upload?token=…` (`video/webm`, ≤500 MB, CORS `*`).

| Action | Where it runs | Params (defaults/bounds) | What it does / reads | Returns |
|---|---|---|---|---|
| `inspect` | target frame | `limit` 1–200 (80) | Lists visible interactive elements (`button,a,input,textarea,select,[role=button],[role=menuitem],[tabindex]`), skips driver overlays | `{title,url,interactive[{selector,tag,text,disabled}],text(≤4000)}` |
| `read` | target frame | `selector` (else `body`), `targetText`, `optional`, `limit` (12000) | Reads normalized text of one element | `{selector,text}` or `{skipped,reason}` |
| `wait` | target frame | `selector` and/or `text`, `timeoutMs` 100–120000 (10000) | Polls every 100 ms for a visible selector containing text, or body text | `{selector,text}`; throws on timeout |
| `announce` | target frame | `text`/`label`, `durationMs` (≥120, 900) | Shows a centered label banner | `{announced}` |
| `click` | target frame | `selector` or `targetText`/`text` (exact then contains match among visible interactive), `label`, `optional`, `settleMs` (≥260, 520) | Scrolls into view, moves cursor (≈650 ms), pulse, `focus()`+`click()` | `{clicked:selector,text}` |
| `type` | target frame | `selector`/`targetText`, `value`, `replace` (default replace; `false` appends), `typingDelayMs` 0–100 (18), `label`, `settleMs` (≥160, 300) | Sets value char-by-char via native setter, dispatches `input` per char and `change` at end; input/textarea/contentEditable only | `{typed:n,selector}` |
| `press` | target frame | `selector` (optional; else activeElement/body), `key` (Enter), `code`, `altKey/ctrlKey/metaKey/shiftKey`, `label`, `optional`, `settleMs` (≥120, 280) | Dispatches synthetic `keydown`+`keyup` | `{pressed,selector}` |
| `chat` | Brainstem frame (default target) | `value`/`text`, `label` ("Giving the Brainstem the job"), `sendLabel`, `typingDelayMs` (5), `timeoutMs` 1000–3600000 (180000) | Types into `#input`, clicks `#send`, detects new `#chat .response-slot[data-request-id]` (≤2 s), waits for `.msg.assistant` (not typing/stream-arriving) `.bubble`; `.msg.system` = error | `{requestId,response,agentLogs}` |
| `surgeon_chat` | shell frame (`target:"shell"`) | `value`/`text`, `label`, `sendLabel`, `typingDelayMs` (5), `timeoutMs` 1000–3600000 (3600000) | Opens `#surgeon` (clicks `#surgeon-tab` or force-opens), counts `#surgeon-log .surgeon-message.assistant/.error`, types `#surgeon-input`, clicks `#surgeon-send`, waits until a new assistant bubble exists and `#surgeon-send` is enabled | `{response}`; throws on new error bubble |
| `set_chat_lease` | Brainstem frame | `token` ("legacy"), `locked` | Installs capture-phase guard blocking trusted `#send` clicks / Enter; banner `#brainstem-beta-chat-lease`; `aria-disabled` | `{leaseCount,locked}` |
| `recording_status` | main process if ffmpeg capture active, else frame | — | Captured: `{active,elapsedMs,encoder:"ffmpeg",error,frameRate,framesWritten,lastFrameAt,pid}`; fallback: MediaRecorder state, bytes, chunks, track settings | see left |
| `screenshot` | main process | — | `capturePage()` → PNG in `captures/`, JPEG preview ≤1280w, Brainstem-frame visible text | `{captureUrl,dataUrl,path,size,visibleText}` |
| `start_recording` | main process (ffmpeg) → fallback in-page MediaRecorder | `maxDurationMs` 1000–600000 (300000), `frameRate` 2–12 (8) | Captured frames piped to ffmpeg (VP9 1800k, realtime); fallback `getUserMedia` desktop source + upload; `REC AUTOPILOT` indicator | `{encoder,frameRate,maxDurationMs,startedAt}` or `{maxDurationMs,mimeType,startedAt,track,fallback}` |
| `stop_recording` | main process | `minimumDurationMs` 0–300000 (0), `recapMode` baseline\|repeat-ephemeral\|stack-churn\|control-handoff | Pads with rotating recap chapter card (`#brainstem-beta-walkthrough-recap`) until minimum, finalizes file (`.tmp` → `.webm`), then screenshot | `{recording{durationMs,frameRate,framesWritten,mimeType,path,sizeBytes,url},screenshot{…}}`; a second stop returns the last recording once |
| `refresh` | Brainstem frame | — | `location.reload()` after 25 ms | `{refreshing:true}` |
| `route_telemetry` | main process | — | `routeManager.telemetrySnapshot()` + `chat_lease_count` (frame) + `navigation_count` (shell) | `{sequence,active_route{url,composition_hash,base_composition_hash},worker_count,stack_count,stack_tree,active_composition_fingerprint,object_count,egg_count,events[],chat_lease_count,navigation_count}` |
| `force_mode` | main process → shell frame | `value` on\|off\|status ("status"), `label`, `idleMs` 1000–600000 (30000) | Edge-glow overlay `#brainstem-ai-force-mode` + tag; `on` arms idle auto-off | `{forceMode:boolean}` |
| `tour` | main process → shell frame | `value` start\|next\|prev\|stop\|goto\|status ("status"), `step` (id or index), `settleMs` 0–5000 (750) | Drives `window.rappShowModeTour`; un-throttles painting, `moveTop()`; waits two rAFs | `{available,running,step,index,total,steps[]}` or `{available,error,steps}` |
| `run` | target frame | `steps[]` 1–40 of step objects (each validated with the same allowlist; `tour`/`force_mode` rejected) | Executes steps sequentially with the per-step semantics above | `{results[]}` |

Known edge: `validateCommand` accepts nested steps of any allowlisted action, but the in-page `perform()` only implements `set_chat_lease, surgeon_chat, chat, recording_status, inspect, read, wait, announce, press, click, type`; nesting `screenshot`, `start_recording`, `stop_recording`, `refresh`, `route_telemetry` inside `run` fails at execution with "Unsupported UI driver action".

Python mirror (`scripts/brainstem_ui_driver_agent.py`, tool `BrainstemUiDriver`): actions `announce, click, force_mode, inspect, press, read, run, screenshot, start_recording, stop_recording, tour, type, wait` (no `chat`/`surgeon_chat`/`set_chat_lease`/`route_telemetry`/`refresh`); snake_case params auto-camelized; `target` enum `brainstem|shell`.

---

## 3. Complete IPC channel list (`electron/main.mjs` ↔ `electron/preload.cjs`)

All renderer→main channels are `ipcMain.handle` (invoke/await) and pass `assertTrustedIpc` (main frame at `ui/index.html` only). There are **no** `ipcMain.on` channels.

| Channel | Preload method | Handler → what it does |
|---|---|---|
| `beta:get-state` | `getState()` | Returns `structuredClone(state)` (`brainstem`, `copilot`, `surgeon`, `uiDriver`, `update`, `url`) |
| `beta:list-agent-files` | `listAgentFiles()` | Identity + active route (`caller_rappid`, `memory_guid`, `active_stack_rappid`, `overlay_stack_rappids`, `stack_lineage`, `stack_tree`, `composition_hash`) + `files[{filename,agents:[],loadable:true,revision,scope}]` from `routeManager.activeAgentFiles()` |
| `beta:read-agent-file` | `readAgentFile(filename, scope)` | Validates `^[A-Za-z0-9_.-]+\.py$`; returns `{filename,scope,content}` from the active composition dir |
| `beta:delete-agent` | `deleteAgent(filename)` | `routeManager.removeActiveAgent` then `startDefault()`; returns removal + `active_route` |
| `beta:export-agent` | `exportAgent(filename)` | Writes active agent source to `~/Downloads` (dedupe suffix, 0600); returns `{canceled:false,filename,path}` |
| `beta:install-frame-bridge` | `installFrameBridge()` | Finds first loopback subframe, `executeJavaScript(BETA_FRAME_BRIDGE_SOURCE)`; `{installed:boolean}` |
| `beta:lineage-command` | `lineageCommand(message)` | `executeLineageCommand` (safe words) → `{intercepted,…,reply,url,compositionHash,…}` |
| `beta:check-for-updates` | `checkForUpdates()` | `handleCheckForUpdates()` → update state object |
| `beta:install-update` | `installUpdate()` | `handleInstallUpdate()` → stages update, quits app |
| `beta:surgeon-send` | `surgeonSend(sessionId, prompt)` | `ensureBrainSurgeon(id).send(prompt)` → `{content}`; creates session (max 12) |
| `beta:surgeon-reset` | `surgeonReset(sessionId=1)` | `surgeon.reset()` (leases released, session disconnected) → `{ok:true}` |
| `beta:surgeon-close` | `surgeonClose(sessionId)` | Deletes from registry, `surgeon.stop()` → `{ok:true}` |
| `beta:store-source` | `storeSource(next)` | `next` ⇒ `setStoreSource({key,url})` (persists `store-source.json`); else returns `{key,url,sources[{key,label,url}]}` |
| `beta:store-install-agent` | `storeInstallAgent(id)` | `installAgentToBrainstem(storeId)` → download (sha) → scoped install → `startDefault()` → `{ok,filename,requested,agent,sha256,persisted,active_route}` |
| `beta:store-list` | `storeList()` | `rappStore.list()` (normalized entries incl. `yanked`, `gated`, `license`) |
| `beta:twin-list` | `twinList()` | `twinManager.list()` descriptors |
| `beta:twin-hatch-egg` | `twinHatchEgg({bytes,filename,instruction})` | `twinManager.hatchEgg` (fail-closed verify) → descriptor |
| `beta:twin-hatch` | `twinHatch(storeId, instruction)` | `twinManager.hatch` → descriptor |
| `beta:twin-chat` | `twinChat(id, prompt)` | `twinManager.chat(id, prompt, {author:"You"})` → kernel response JSON |
| `beta:twin-run` | `twinRun(id, instruction)` | `twinManager.run` (bounded canned loop) → descriptor. **Unused by renderer.** |
| `beta:twin-loop` | `twinLoop(id, goal)` | Kicks `twinManager.loop` (non-blocking) → `{ok:true,looping:id}` |
| `beta:twin-close` | `twinClose(id)` | `twinManager.close(id)` → `{ok:true}` |
| `beta:twin-deploy-copilot-studio` | `twinDeployCopilotStudio(options)` | `hatchCopilotStudioTwin({displayName,environment,agents})` → descriptor. **Unused by renderer** (Surgeon tool path instead). |
| `beta:twin-popout` | `twinPopOut(id)` | `popOutTwin(id)` opens child BrowserWindow → `{ok:true}` |
| `beta:twin-inject-ui` | `twinInjectUi(id)` | `injectTwinUi(id)` document.writes custom UI into the herd iframe. **Unused by renderer.** |
| `beta:open-auth` | `openAuth({url,id})` | `shell.openExternal` (http(s) only; twin's pending auth URL by id) → `{ok,opened}` |

Main → renderer (`webContents.send`, subscribed via preload `on*`):

| Channel | Preload | Payload |
|---|---|---|
| `beta:state` | `onState` | full state snapshot on every change |
| `beta:surgeon-event` | `onSurgeonEvent` | `{timestamp,type,sessionId,…}` types: ready, response-start, delta, tool-start, tool-complete, artifact, lease, done, error, reset |
| `beta:twin-event` | `onTwinEvent` | `{timestamp,type,id,…}` types: twin-hatched, twin-status, twin-log, twin-message, twin-closed, twin-needs-auth (payload passed through `redactSensitiveValue`) |
| `beta:twin-focus` | `onTwinFocus` | `{id}` (from OS notification click) |
| `beta:open-update` | `onOpenUpdate` | none (open the in-frame update panel) |

Iframe ↔ renderer `postMessage` protocol (not IPC, but part of the bridge contract): `rapp-beta:check-updates`, `rapp-beta:install-update`, `rapp-beta:toggle-explorer`, `rapp-beta:explorer-state`, `rapp-beta:update-state`, `rapp-beta:open-update`, `rapp-beta:lineage-chat` / `-result`, `rapp-beta:lineage-confirmation` / `-ack`, `rapp-beta-delete-agent` / `-result`, `rapp-beta-export-agent` / `-result`.

---

## 4. UI element ids / selectors scripts and tests rely on

### 4.1 Electron shell (`ui/index.html`, `target:"shell"`)
`#brainstem` (iframe), `#splash`, `#error`, `#intro`, `#intro-title`, `#show-mode-interview-prompt`, `#enter`, `#explorer`, `#explorer-refresh`, `#explorer-close`, `#agent-tree` (read by walkthrough), `#agent-viewer`, `#agent-viewer-tab`, `#agent-viewer-empty`, `#agent-source`, `#explorer-status`, `#surgeon-tab`, `#surgeon`, `#surgeon-model`, `#surgeon-herd-btn`, `#surgeon-new`, `#surgeon-close`, `#surgeon-tabs`, `#surgeon-log`, `#surgeon-input`, `#surgeon-mode`, `#surgeon-send`.
Dynamic: `#surgeon-herd` (`.hbar .hstore|.hnew|.hclose`, `.herd-grid`), `.herd-tile` (`.hh .tt|.hst|.cl|.sp`, `.htrans`, `.hcomp textarea|button`), `.herd-tile.twin[data-twin-id]` (`.twin-badge`, `.tw-loop`, `.tw-pop`, `.twin-meta`, `.twin-chat`, `.tw-msg.(activity|user|assistant|driver|error)`, `.tw-turn.(self|driver)`, `.tw-who`, `.twin-signin`, `.twin-comp textarea`, `.tw-send`), `.store-picker` (`.store-sources`, `.store-source-tab(.active)`, `.store-custom-row input|button`, `.store-row[data-store-id]`, `.store-hatch`, `.store-install`), `.herd-error`, `.surgeon-session`, `.surgeon-message.(user|assistant|error)` (polled by `surgeon_chat`), `.surgeon-thinking`, `.surgeon-tool[data-tool-name][data-tool-call-id][data-active]`, `.surgeon-tool-status`, `.surgeon-artifact(.link)`, `.surgeon-empty`, `.surgeon-starters button`, `.show-mode-preview[data-show-mode-tour]`, `.deploy-copilot-studio`, `.surgeon-tab(.active) .tt|.cl`, `.surgeon-new`, `[data-show-mode-tour]`.
Body classes: `explorer-open`, `surgeon-open`, `surgeon-herd-open`, `smt-running`, `smt-intro-step`.
Globals: `window.brainstemBeta` (preload API), `window.rappShowModeTour`, `window.__brainstemBetaNavigationCount`.
Tour DOM: `#smt-root`, `#smt-shade`, `#smt-hole`, `#smt-ring`, `#smt-card` (`.smt-bar`, `.smt-head .smt-step|.smt-x`, `.smt-body`, `.smt-notes`, `.smt-foot .smt-back|.smt-notes-toggle|.smt-next`, `.smt-keys`), `#smt-cursor`, `#smt-pulse`, `.smt-ghost[data-pane=explorer|center|surgeon]`, `#smt-css`.

### 4.2 Brainstem (Grail) iframe — kernel ids the bridge/driver depend on (`rapp_brainstem/index.html`)
`#input`, `#send`, `#chat`, `#chat .response-slot[data-request-id]`, `.msg.assistant`, `.msg.system`, `.typing-indicator`, `.stream-arriving`, `.bubble`, `.agent-logs`, `header .logo`, `header .controls`, `#vscode-link`, `#agent-list-ul`, `.agent-title`, `.agent-name[title]`, `.export-btn`, `.del-btn`, button text `Clear`, global `appendMsg()`, `loadAgentsList()`.
Injected by the bridge: `.beta-app-wrapper.beta-frame-menu`, `#beta-app-btn`, `#beta-app-panel(.open)`, `#beta-check-updates`, `#beta-install-update`, `#beta-update-status[data-phase]`, optional `#beta-update-message|-detail|-source|-guidance`, `.beta-agent-icon-button`, `body.beta-app`, `header .logo[data-beta-explorer-toggle]`, globals `window.__rappBetaFrameBridge`, `window.__rappFrontierExportRedaction`.
Injected by the driver: `#brainstem-ai-driver-style|-cursor|-pulse|-label`, `.brainstem-ai-driver-target`, `#brainstem-beta-chat-lease`, `#brainstem-beta-recording-indicator`, `#brainstem-beta-walkthrough-recap`, `#brainstem-ai-force-mode` (+`-style`, `.brainstem-ai-force-edge|-tag|-dot|-text`), globals `window.__brainstemAiDriver`, `window.__brainstemBetaRecording`, `window.__brainstemBetaLastRecording`.

### 4.3 Twin frames / pop-out
`#__rappViewStyle`, `#__rappViewToggle`, `html[data-rapp-view=mobile|full]`, `window.__rappForceModeCapable`, `html[data-rapp-force-mode="ready"]`. Rapplication UIs: agent-migration `#payload #analyze #convert #status #out`; agentic-app-studio `#agentName #agentSource #appName #envUrl #status #main #out #previewWrap #previewUrl #preview #driveText #driveSend #driveLog`.

### 4.4 Landing page (`beta/index.html`)
`#unix-command`, `#windows-command`, `#copy-unix`, `#copy-windows`, `#unix-script`, `#windows-script`, `#release-status|-version|-commit|-repo`, `#release-link-top|-bottom`, `#resolved-date`, `#load-error`, `#source-link`, `#library-link`.

### 4.5 Ids asserted by existing tests
`#surgeon-herd`, `#input`, `#brainstem`, `#beta-app-btn` (others are asserted as source regexes, e.g. `twinPopOut:`, `UI_STAGE_ACTIONS`).

---

## 5. Determinism blockers and existing stub/fake/replay facilities

### 5.1 Real model (GitHub Copilot)
- **Surgeon** every `send` → Copilot SDK session → real model (`approveAll`). No replay/recording facility exists. Tests inject a fake `runtime.createSession` (brain-surgeon.test) returning canned `sendAndWait`.
- **Brainstem kernel `/chat`** (visible chat, `chat` drive action, twin chat/run/loop planner) → the Grail kernel calls the GitHub Copilot API with the user's token. No fake model, no `GITHUB_MODEL` mock; only the kernel's own device-code auth.
- Consequently every script in §1.12 except `show-mode-capture.mjs`, `walkthrough-gate.mjs`, `walkthrough-certify.mjs`, `package-gate.mjs` needs a signed-in Copilot and a live model; the walkthrough prompt relies on the model following six chapters and emitting exact markers (`FIVE_MINUTE_WALKTHROUGH_COMPLETE`, `LEARNED_AND_TAUGHT:RAPP_READY`, `STACK_CHURN_READY`, `UPDATE_STATUS=` …).
- Twin `loop()` requires two model chains (planner Brainstem + twin worker) plus regex detection of `DONE`/auth requests.

### 5.2 Network
- Store catalogs (AIBAST Pages registry, Frontier store, Public RAR, custom) and raw.githubusercontent singleton/egg/UI downloads — `RappStoreClient` takes `fetchImpl` (used by rapp-store.test with an in-memory `fakeStore`). Main constructs clients with global `fetch`; no env override for the catalog URL other than the `custom` source (may be `http://127.0.0.1…` — a local fixture server is therefore possible).
- Update check/install: `git fetch` against GitHub (`runGit` injectable; update-manager.test passes fake git), `install.sh` downloads node tarballs + SHASUMS, npm registry, Electron.
- Molter (`AIBAST_REGISTRY` env in the agent), Agentic App Studio RAR fetches, PAC (`pac auth …`), Copilot Studio tenant.
- `frontier.sh/.ps1`: GitHub REST API + raw.
- Copilot CLI itself talks to GitHub.

### 5.3 Time
- Filenames embed ISO timestamps (`captures/brainstem-<iso>.png`, `recordings/brainstem-<iso>.webm`, walkthrough `run_id`, `.tmp` suffixes with pid).
- `createdAt` on rings (`LineageStore({now})` injectable; lineage-store.test uses a ticking fake), `createdUtc` on eggs (`packageAgent` uses `new Date()`; `tools/rapp1` vectors pin fixed UTC), telemetry timestamps, twin `createdUtc`, `startedAt` in `ui-driver.json`.
- Timers: cursor idle 4 s, force-mode idle 30 s, chat poll 150 ms, `wait` poll 100 ms, heartbeat 15 s, health wait 90 s (500 ms interval), `waitForWorkerIdle` 30 s, composition negative-cache TTL 60 s, recording auto-stop, walkthrough-gate "evidence is fresh" ≤ 6 h, "artifact created during this run" ±5 s, update-runner parent wait 120 s, log flush 5 s.
- `package-gate` "packaged app is newer than beta source" compares mtimes.

### 5.4 Randomness / identity
- `randomUUID` for caller/stack RAPPID anchors (`mintRappid` accepts `uuidAnchor`), ephemeral composition nonce, chat-lease tokens, staging dir names; `randomBytes` for driver tokens and artifact ids; twin ids `<slug>-<seq>` (per process), Surgeon ids (renderer seq, persisted). Lineage ring/ancestor ids are deterministic (content + filename).
- `compositionHash` is deterministic for a given identity + bytes; `identity.json` makes every install unique — a fresh `BRAINSTEM_BETA_HOME` yields a fresh RAPPID/memory GUID.

### 5.5 Ports / processes
- Workers and twins use OS-allocated loopback ports; the default route reuses any healthy server already on `config.url` (default `:7071`) — a user-run `brainstem.py` on 7071 is adopted, not owned (`BrainstemProcess.start` → `reused:true`). `BRAINSTEM_BETA_PORT` changes the default. UI-driver port is random (read from metadata).
- Orphaned twin/worker processes from a crashed session are not reaped (only their dirs are wiped).
- Agentic App Studio spawns `serve.py`; PAC/ffmpeg/ffprobe/git/python are external executables (`BRAINSTEM_BETA_FFMPEG`, `BRAINSTEM_BETA_FFPROBE`, `BRAINSTEM_BETA_PAC`, `BRAINSTEM_BETA_GIT_EXE`, `BRAINSTEM_BETA_NODE_EXE`, `BRAINSTEM_BETA_PYTHON` overrides).
- Electron single-instance lock: a second launch with the same user data dir exits.

### 5.6 The user's own home
- `~/.brainstem` (kernel source + `agents/` — **delete-agent writes here**, `venv`, `.copilot_token`, `logs/`), `~/.brainstem/beta-launcher` (all beta state), `~/.rapp/lineage` (shared across installs unless `RAPP_LINEAGE_HOME`), `~/.rapp/<rapplication>` device stores, `~/Downloads` (export target; default `local.settings.json` location — `BRAINSTEM_BETA_LOCAL_SETTINGS`), `~/.dotnet/tools/pac`, `~/.local/bin`, `~/Applications`, Electron `userData` (localStorage: intro, surgeon sessions, panel states, tour notes).
- Isolation knobs: `BRAINSTEM_HOME`, `BRAINSTEM_BETA_HOME`, `BRAINSTEM_BETA_SOURCE_DIR`, `BRAINSTEM_BETA_PYTHON`, `BRAINSTEM_BETA_UI_DRIVER_FILE`, `RAPP_LINEAGE_HOME`, `RAPP_MOLT_LINEAGE=0`, `RAPP_BASELINE_SAFEWORD`, `RAPP_RESTORE_WORD`, `BRAINSTEM_BETA_RAPP_OWNER`, `BRAINSTEM_BETA_HEADLESS=1`, `BRAINSTEM_BETA_SMOKE_EXIT_MS`, `BRAINSTEM_BETA_RUNTIME_DIR` (tests: where the Grail kernel lives), `BRAINSTEM_BETA_E2E_TIMEOUT_MS`, `BRAINSTEM_BETA_WALKTHROUGH_TIMEOUT_MS`, `BRAINSTEM_BETA_REPO_URL`, `BRAINSTEM_BETA_REF`, `BRAINSTEM_BETA_UPDATE_REF`, `BRAINSTEM_BETA_COMMIT`, `BRAINSTEM_BETA_RELEASE_TAG`, `BRAINSTEM_BETA_RUNTIME_VERSION_URL`, `BRAINSTEM_BETA_NODE_VERSION`, `BRAINSTEM_BETA_NO_LAUNCH`, `BRAINSTEM_BETA_LAUNCHER` (path to `launch.sh` used by scripts), `BRAINSTEM_BETA_BOOTSTRAP_URL`, `BRAINSTEM_BETA_INSTALLER_PATH`, `RAPP_FRONTIER_REPO`, `RAPP_FRONTIER_RESOLVE_ONLY`, `RAPP_PROTOCOL` (build_rapp_egg), `AGENT_MIGRATION_HOME`, `AGENTIC_APP_HOME`, `AGENTIC_APP_LOCAL_AGENTS`, `MOLTER_HOME`, `TOASTER_HOME`, `UI_SMITH_HOME`. Electron's own userData dir (localStorage) has no knob.
- **No `.env`-style fake for the Copilot token**: `CopilotRuntime` reads `~/.brainstem/.copilot_token`; an empty/absent token = `signed-out` phase.

### 5.7 Existing fakes, stubs, vectors (reusable by a regression harness)
- `tools/rapp1/conformance-vectors.json` (canonical/H/identity/frame/egg vectors; outside `beta/`).
- brain-surgeon.test: fake `runtime.createSession` + fake `uiCommand` + fake `routeManager`.
- rapp-store.test: `fakeStore()` in-memory fetch with sha/gated/yanked toggles.
- update-manager.test: fake `runGit`; `prepareUpdate` accepts `spawnProcess`; update-runner.test runs the real runner against temp git repos (POSIX only).
- route-manager.test / molt-lineage-integration.test: `compositionValidator: () => true`, `moltVerifier: () => true`, `fakeWorkerProcess()` via `manager.createWorkerProcess`, direct `manager.workers.set(...)` stubs; "real Grail boot" tests need `BRAINSTEM_BETA_RUNTIME_DIR` + a python (`~/.brainstem/venv`, or `/usr/bin/python3`).
- brainstem-process.test: `waitForHealth({probe})` injection. log-redaction.test: `fakeKernelSource` spawns a fake python kernel to stream stdout/stderr.
- lineage-store.test: injectable `now`; temp roots.
- frontier-rapplication-hardening.test: loads the Python agents through `spawnSync(python …)` with a stub `BasicAgent`.
- ui-driver-server.test: asserts source/structure only (no HTTP server spun up in tests).
- Headless/packaged smoke: `BRAINSTEM_BETA_HEADLESS=1 BRAINSTEM_BETA_SMOKE_EXIT_MS=3000 BRAINSTEM_BETA_HOME=<tmp>` (package-gate).
- No fake Copilot model, no `/chat` replay server, no fixture twin, no Playwright/DOM harness for `renderer.js` or `show-mode-tour.js` (tests regex the source).

---

## 6. What nobody tests today

1. **The renderer at runtime.** `ui/renderer.js` (1.5k lines: explorer tree rendering, tab/herd state machine, localStorage persistence/quota fallback, twin tiles, store picker, egg drag veil/drop, event routing by `sessionId`) has zero executed coverage; all "surgeon-multichat"/"twin-manager" UI tests are regex checks on the source.
2. **Live `/v1/command` server behavior** — auth rejection, body limit, heartbeat, artifact serving/expiry, recording upload, `twin:` targeting, `target:"shell"` — is not exercised by a test (ui-driver-server.test only inspects source); only the model-dependent scripts hit it.
3. **Multi-Surgeon concurrency in practice**: two chats delegating at once, the UI stage queue ordering, a stage command failing while another is queued, `reset` while a delegation holds the lease, hitting the 12-session cap.
4. **Twin concurrency**: twin + main route swap at the same time, 8-twin cap, `close()` during `loop()` round (only the flag is tested), OS notification flow, pop-out window lifecycle, `twinInjectUi`, `twinRun`, `twinDeployCopilotStudio` IPCs (renderer never calls them; no tests exercise the IPC handlers).
5. **Egg drop end-to-end** (renderer drop → IPC → `hatchEgg` → worker boots) and `build_rapp_egg.mjs` output re-hatching.
6. **Show Mode tour logic executed** (step positioning, pane restore, keyboard handling, `?tour=show-mode` autostart) — only static greps; `show-mode-capture` requires a live window.
7. **Frame bridge in a real iframe**: fetch interception for `/chat/stream`, fail-open on timeout, lineage confirmation re-delivery across a route change, export/delete buttons, three-dot menu state transitions, export-redaction Blob wrapper inside the Grail page (tested only in a `vm` context).
8. **Update UI flow**: `handleInstallUpdate` → quit → runner → `consumeUpdateResult` phases on next launch (`success`, `error` with/without rollback); `dirty`/`blocked` rendering; menu item enable/disable.
9. **Delete of a global agent through the UI** actually removing a file from `~/.brainstem/src/rapp_brainstem/agents` and the subsequent route swap (unit test covers the manager only).
10. **Store picker source switching** (persisting `store-source.json`, custom loopback URL, yanked filtering in the DOM, `⇣ Install` success banner) and `installAgentToBrainstem` as wired in main.
11. **Copilot Studio path beyond unit stubs**: PAC device login lifecycle (`startDeviceLogin` polling, kill on stop), `ensure_copilot_studio_deploy_agents` materialization permissions on a real betaHome, the deploy twin's instruction loop, Draft-only guarantee at runtime.
12. **Lineage safe words through the real chat** (typed `baseline`/`restore` in the Grail composer, reply rendering, route URL change) — only `executeLineageCommand` is unit-tested.
13. **Locus policy `pinned`** has no user-facing entry point and no integration test through composition.
14. **`materializeExternalAgentSet` "refuse to mutate after publication"** path and twin lineage overlay with a real worker.
15. **Ring-1 ContextMemory python** (`scan_broken_agents`, `_operating_context_block`) has no dedicated test in `beta/tests`; seeding silently skips when the Grail baseline sha differs from the pin (no test that a newer Grail still gets ring 1).
16. **Recording fallback path** (`getUserMedia` MediaRecorder + `/v1/recording-upload`) and `recording_status` for the fallback; ffmpeg absent at runtime.
17. **`BrainstemProcess` reuse of an already-running 7071 server** (owned=false) and its interaction with `stop()`/route retirement.
18. **Navigation guards** (`will-frame-navigate` allowing arbitrary loopback subframes, window.open deny) and `assertTrustedIpc` rejection from the iframe.
19. **Windows**: update-runner tests are skipped off-POSIX; `install.cmd`, shortcuts, `launch.cmd`, `ProgramFiles` git discovery, CRLF handling end-to-end (only identity canonicalization is tested).
20. **Installers on a clean machine** (`install.sh`/`install.cmd`/`frontier.*`) — RELEASING §5 prescribes a manual clean-HOME run; no automated job.
21. **Walkthrough scripts themselves** (`walkthrough-via-chat` control flow, `walkthrough-gate` requirement logic, `walkthrough-certify`, `frontier-acceptance`, `drive-via-chat`, `brainstem-chat`, `surgeon-chat`, `show-mode-capture`) have no unit tests except the pure `evaluateControlHandoff` helper.
22. **Telemetry ring overflow** (>500 events) and `telemetrySnapshot` against many workers; `retireInactiveWorker` timeout (“still handling a request”) path.
23. **Rapplication custom UIs** (`agent-migration/index.html`, `agentic-app-studio/index.html` AgenticDrive bus) and `serve.py` local server lifecycle (`serve`/`stop_server`).
24. **Accessibility/keyboard**: ⌘/Ctrl+Enter send, Enter-to-send in tile composers, tour keys, logo Enter/Space — untested.
25. **Shutdown ordering** (`before-quit` with in-flight recording/loop/update) and `uiDriver.stop()` metadata ownership check.
